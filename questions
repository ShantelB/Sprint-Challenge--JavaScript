1. Describe the biggest difference between `.forEach` & `.map`.

.forEach will allow a callback function to mutate the current array. 
.map will instead return a new array.


2. What is the difference between a function and a method?

A function is a piece of code that is called by name and can be passed data and return data.
A method is a piece of code that is called by a name that is associated with an object.

3. What is closure?

It’s a function inside a function

4. Describe the four rules of the 'this' keyword.

Window Binding - Default unless in strict mode
New Binding - Using the "new" keyword to construct a new object
Explicit Binding - Apply and call will invoke immediately. Bind has to be called in later.
Implicit Binding - Look to the left of the dot for the object

5. Why do we need super() in an extended class?

It calls to the to the parent. It gets  rid of .call =Object.create